#!/usr/bin/env bash
# Moved to tools/hcc_v2v/scripts/setup_v2v_network.sh (it is used for in-car
# V2V testing, not just HIL). This stub forwards for old docs/scripts.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)/../../../hcc_v2v/scripts/setup_v2v_network.sh" "$@"
