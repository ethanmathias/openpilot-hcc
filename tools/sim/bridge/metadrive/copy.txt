How to run on your MetaDrive PC

Terminal 1:
./tools/sim/launch_openpilot.sh
Terminal 2:
./tools/sim/run_bridge.py --scenario hccc_step
This runs a deterministic lead profile (~22 m/s -> ~10 m/s -> ~22 m/s) via liveTracks, so radard produces radarState.leadOne for your hCCC logic.

Validation done

Syntax check passed for modified Python files:
python3 -m py_compile on all touched .py files in tools/sim.
I could not run full simulator runtime tests in this environment (MetaDrive not installed here).